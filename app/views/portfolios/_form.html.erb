<%= form_for @portfolio do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :cash, "Initial Cash" %>
  <%= f.text_field :cash %>

  <%= f.submit "Add Portfolio", class: "btn btn-large btn-primary" %>
<% end %>